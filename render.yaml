services:
  - type: web
    name: outletv1-backend
    env: node
    plan: free
    buildCommand: cd outlet_backend && npm install
    startCommand: cd outlet_backend && npm start
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: PORT
        value: 10000
